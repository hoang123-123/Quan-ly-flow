# --- CẤU HÌNH API POWER AUTOMATE ---

# 1. API Lấy danh sách Flow (Trigger từ Power Automate)
VITE_URL_LIST_FLOWS=https://prod-XXX.southeastasia.logic.azure.com:443/workflows/...

# 2. API Lấy Access Token (OAuth2)
# Bạn có thể dùng URL đầy đủ hoặc chỉ điền Tenant ID
VITE_URL_GET_TOKEN=https://login.microsoftonline.com/YOUR_TENANT_ID/oauth2/v2.0/token
VITE_TENANT_ID=YOUR_TENANT_ID

# 3. API Lấy Lịch sử chạy & Metadata (Power Platform / Flow API)
# Thường là API chính thức của Microsoft: https://api.flow.microsoft.com
VITE_URL_GET_HISTORY=https://api.flow.microsoft.com/providers/Microsoft.ProcessSimple/environments/{environmentId}/flows/{flowId}/runs?api-version=2016-11-01
VITE_URL_GET_METADATA=https://api.flow.microsoft.com/providers/Microsoft.ProcessSimple/environments/{environmentId}/flows/{flowId}?api-version=2016-11-01

# --- THÔNG TIN AUTHENTICATION (Azure AD App) ---
VITE_CLIENT_ID=YOUR_CLIENT_ID
VITE_CLIENT_SECRET=YOUR_CLIENT_SECRET
VITE_SCOPE=https://service.flow.microsoft.com/.default
